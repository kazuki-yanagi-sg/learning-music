/**
 * ピアノロール / ドラムグリッド共通レイアウト定数
 *
 * メイン編集画面では DrumGrid と PianoRoll を縦に並べて表示する。
 * 各レーン左側のラベル領域（gutter）の幅がズレると、時間軸（拍）の開始 x がズレ、
 * 小節ルーラーの縦ラインがレーン間で揃わなくなる。
 *
 * これを防ぐため、左ラベル領域の幅を 1 箇所で定義し、両コンポーネントが共有する。
 * 値はドラムの文字ラベル（例: 「ハイハット(O)」）が切れない 80px に統一する。
 */

// 左ラベル領域（gutter）の幅（px）。DrumGrid / PianoRoll で必ず一致させる。
export const LABEL_GUTTER_WIDTH = 80
